type: my-project

components:
  myRole:
    type: aws-iam-role
    inputs:
      name: some-role
      service: lambda.amazonaws.com
  mySecondRole:
    type: aws-iam-role
    inputs:
      name: some-role
      service: lambda.amazonaws.com
