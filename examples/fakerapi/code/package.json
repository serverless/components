{
  "name": "fakerapi",
  "version": "1.0.0",
  "description": "An example REST API application composed of a few serverless components. The REST API exposes one route `POST /faker` that takes a `category`, a `item` and an optional `locale` attribute, and returns fake data. The application is deployable to AWS.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0"
  }
}
