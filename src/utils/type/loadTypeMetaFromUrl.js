/**
 * @param {string} url the url to download the type archive from
 * @param {*} context
 * @returns {{
 *   root: string,
 *   type: string
 * }}
 */
const loadTypeMetaFromUrl = async (url, context) => {}

export default loadTypeMetaFromUrl
