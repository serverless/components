name: TwilioProvider
extends: Provider
version: 0.3.0
main: ./dist/index.js

accountSid: ${inputs.accountSid}
authToken: ${inputs.authToken}

inputTypes:
  accountSid:
    type: string
    require: true
  authToken:
    type: string
    require: true
