// const AWS = require('aws-sdk')

// const { createVpc, createDefaultVpc, deleteVpc } = new AWS.EC2()

const deploy = async () => {
  return {}
}
const remove = async () => {
  return {}
}
module.exports = {
  deploy,
  remove
}
