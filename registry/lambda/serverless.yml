type: lambda

inputs:
  name: default-lambda-name-goes-here
  memory: 512
  timeout: 100
  description: default description goes here
  handler: code.handler
  role: ${myRole.outputs.arn}

components:
  myRole:
    type: iam
    inputs:
      name: ${lambda.inputs.name}
      service: lambda.amazonaws.com
