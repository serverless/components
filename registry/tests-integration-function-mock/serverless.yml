type: tests-integration-function-mock

inputs:
  name: ${FUNCTION_NAME}
  memorySize: 512
  timeout: 100
  role: ${myRole.outputs.id}

components:
  myRole:
    type: tests-integration-iam-mock
    inputs:
      name: ${tests-integration-function-mock.inputs.name}}
      service: my.function.service
