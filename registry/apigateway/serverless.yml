type: apigateway

inputs:
  name: apigateway
  method: POST
  path: /hello
  lambda: ""
  role: ${myRole:arn}

components:
  myRole:
    type: iam
    inputs:
      name: ${parent:name}
      service: apigateway.amazonaws.com
