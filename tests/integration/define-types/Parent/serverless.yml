extends: Component
name: Parent

inputTypes:
  bar:
    type: string

bar: ${inputs.bar}

components:
  childA:
    type: ../Child
    inputs:
      foo: abc
  childB:
    type: ../Child
    inputs:
      foo: def

objectProp:
  childC:
    type: ../Child
    inputs:
      foo: ghi

arrayProp:
  - childD:
      type: ../Child
      inputs:
        foo: jkl

deeply:
  nested:
    childs:
      childE:
        type: ../Child
        inputs:
          foo: jkl
